
server:
  port: 8001

logging:
  level:
    hu.flowacademy.foodOrder: TRACE

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/foodOrder
    username: root
    password: P4ssword+
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
